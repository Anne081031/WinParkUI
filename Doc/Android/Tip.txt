Android Network Library
AndroidAsync kryonet

Netty Vert.x Grizzy Mina


////////////// WinTone Android SN
XRYQVURUVZQY5TIYY1JFYY6BE
XRSD9URRDLPY64TYY2NJYY9KG
///////////////////////////////////////

Wine, MinGW GCC/MSYS, Cygwin GCC, POSIX

ACE ICE POCO 
Boost
unixODBC iODBC

http://www.winehq.org/ Wine 在类Unix上运行Windows程序


深入理解Android 卷1 对Framework层的分析为主，兼顾Native层和Application层 / 深入理解Android 卷2 重点是Android Framework的Java层 未下载
//////////////////////
DSRC的ETC中RSU和OBU Electronic Toll Collection/ OnBoard Unit/RoadSide Unit/ Dedicated Short Range Communication
//////////////////////

PHP yiiframework
JACOB is a JAVA-COM Bridge that allows you to call COM Automation comp
Desktop App应用网络(Web)部署 .Net ClickOnce / Java JNLP 
AWT(Abstract Windowing Toolkit)-->Swing-->SWT(Standard Diwget Toolkit)
///////////////////////////////////////////////////////
malloc/free 库函数 内存分配与释放


new/delete 运算符 编译器会做特殊处理
new --> sizeof -->operator new(可以重置) --> malloc(或其他的分配函数) --> HeapAlloc -- > constructor
delete --> destuctor -- >operator delete(可以重置) -->free(或其他函数) --> HeadpFree

 _beginthreadex/_endthreadex CreateThread/ExitThread
CRT是线程出现之前实现的，_beginthreadex先把全局成员私有化，成员结构实例化，调用CreateThread，保存到TLS,这样每个线程都有自己全局数据存储区；_endthreadex先释放TLS，调用ExitThread
如果你正在编写C/C++代码，决不应该调用CreateThread； 应该使用Visual C++运行期库函数_beginthreadex。

当开发C/C++程序要使用CRT时，_beginthreadex/_endthreadex，否则可以不用 Win32API MFC等等


Type的含义 例如 class struct enum int float string等等

new 操作符 先为对象分配内存即生成对象,然后执行构造函数
delete 操作符 先执行析构函数，再释放内存即销毁对象

构造、析构函数是递归调用方式，继承关系
Constructor( )
{
	ParentConstructor( );
	IncludedObjectConstructor( );
	其他代码
}

Destructor( )
{
	其他代码
	IncludedObjectDestructor( );
	ParentDestructor( );
}

Object 布局 所占内存大小要考虑对齐与优化，空类默认占1字节
pVTavle 虚函数表指针 第一个成员
其他按顺序声明的成员
int a;
CMyTest b;
char c;

成员函数数存放在函数区，不占对象尺寸

public protected private 继承会把能继承的成员访问限制大于该修饰的限制修改为继承修饰限制,virtual base的运用/菱形，保证只有一个q实例存在

   q
c     b
   d

namespace nest template friend(函数 成员函数 类)，一类的友元能方问该类一切成员

同一类的不同实例共享同一虚函数表，从最高一级有虚函数的开始，以后每级子类（无论有无虚函数，或重置）都有自己的虚函数表,虚函数表有自己的存储空间，多继承。
虚函数表不同编译器实现方式不一样

/////////////////////////////////////////////////

http://docs.oracle.com/javase/7/docs/ Java核心

http://www.web3d.org OpenGL DirectX  Java3D Web3D VRML WebCam

http://www.eoeandroid.com/ 主要中文网站

http://www.androidjavadoc.com Class Hierarchy


CrossPlatform Middleware / framework

Web Techlonogy 实现平台
Apps--->Middleware--->OS 不同OS有不同隔离层件
PhoneGap(开源)、Titanium（商业) HTML/CSS(UI) + JavaScript(Call Platform API) / Browser作为App Container
Rhodes(商业) Ruby + Rails / Web App server 作为App Container

Android AVD SDK ADT

java.lang.Thread Daemon/User Thread
java.until.concurrent.* 线程池

明确主线程和子线程的职责。主线程的职责是创建、显示和更新UI控件、处理UI事件、启动子线程、停止子线程;子线程的职责是计算逝去的时间和向主线程发出更新UI消息，而不是直接更新UI。在Android中，线程有两个对象—消息(Message)和消息队列(MessageQueue)可以实现线程间的通信。

Android http://developer.android.com/guide/components/fundamentals.html
Handler Message Looper MessageQueue HandlerThread
Looper/Class used to run a message loop for a thread. 

Android Application--->x.apk( Android Package ) Install App-->Process
每个App有自己的Sandbox，唯一的User ID，进程有自己的VM, 应用可以共享UID(同一USer运行)、Process(在同一Process内运行)、VM(多Process共享同一VM)，安装时要授予App能访问Device的那些资源的权限

Application components are the essential building blocks of an Android application.
Activities---An activity represents a single screen with a user interface. Activity互相独立
Services-----A service is a component that runs in the background to perform long-running operations or to perform work for remote processes. A service does not provide a user interface.
Content providers---A content provider manages a shared set of application data. 
Broadcast receivers---A broadcast receiver is a component that responds to system-wide broadcast announcements. 

Three of the four component types—activities, services, and broadcast receivers—are activated by an asynchronous message called an intent.
Content providers,it is activated when targeted by a request from a ContentResolver. 

Don't have a single entry point(there's no main() function)
A unique aspect of the Android system design is that any application can start another application’s component，运行在另一个Process内(App-->SendMessage-->OS-->Start App Component). 
The Manifest File,AndroidManifest.xml

http://sqlite.org
Android开发中使用SQLite数据库 SQLiteOpenHelper /data/data//databases文件夹中 android.database 一个最方便的 SQLite 客户端是 FireFox SQLite Manager 扩展，它可以跨所有平台使用。Berkeley DB

Dalvik虚拟机
大多数虚拟机包括JVM都是一種堆疊機器，而Dalvik虚拟机则是基于寄存器的。两种架构各有优劣，一般而言，基于栈的机器需要更多指令，而基于寄存器的机器指令更大。DDMS( .dex)
dx 是一套工具，可以將 Java .class 轉換成 .dex 格式. 一個dex檔通常會有多個.class。由於dex有時必須進行最佳化，會使檔案大小增加1-4倍，以ODEX结尾。
Dalvik虚拟机早期并没有使用JIT（Just-In-Time）技术. 从 Android 2.2 开始, Dalvik 虚拟机也支持 JIT.

DDMS Dalvik Debug Monitor Service
ADB Android Debug Bridge
AAPT Android Asset Packaging Tool
AIDL Android Interface Definition Language

SD Secure Digital Memory Card 外部存储卡

android.content.Context.getSystemService( String name ); 获取操作系统提供的服务，GPS Multimedia eg.

Java调试模式
JPDA(Java Platform Debugger Architecture)-->JVMTI(JVM Tools Interface)/JDI(Java Debug Interface)/JDWP(Java Debug Wire Protocol) 
Eclipse( JDWP Debugger )---DDMS---ADB----Device( ADB--- APP VM )
ABD C/S Model

Applications Market 软件交易市场，市场交易平台一般有相关平台公司(Google,Microsoft,HP,IBM,Oracle,Apple,Amazon etc.),开发者开发相关平台的应用程序，测试后发布到交易平台，供用户免费使用费或购买使用，所得费用一般由交易平台公司与应用卖家按相关协议分成。云计算(IDC)是卖服务 IAAS PAAS( AppEngine WindowsAzure OpenStack AWS) NAAS SAAS(Office Or Other Clound Version Software)

2.3.3-2.3.7
4.0.3-4.0.4

Activity(Page) ActivityGroup Window FlagWindow FullScreen Interaction Task BackStack LifeCircle StateChart StateMachine Fragment FragmentManager
View Widgets(UI Component) ViewGroup/Layout/Container
Intent/Intent Filters

ContentProvider/向外提供数据  ContentResolver/访问ContentProvider提供的数据

setContentView 使用Layout XML文件描述界面布局，达到实现代码与UI分离的目的
Interface做参数的优势，可以接受任何实现了该Interface的实例
LayoutInflater 把Layout XML转化为Layout类的实例
				UITree
				  
			      MainWindow(Activity/Page)
				  |
			         \|/
                          --------------------
                          |                  |
			 \|/  		    \|/

		     SingleView         ViewGroup(Layout/Container)

						 |
						\|/
					 ------------------
					 |	          |
					\|/		 \|/
				     SingleView	     ViewGroup(Layout/Container)
							       |
							      \|/
							   SingleView				


Spinner<--->ArrayAdapter<--->ArrayList<String>
DataControl <--->DataAdapter<--->DataSource

android.content.Context Interface to global information about an application environment. This is an abstract class whose implementation is provided by the Android system. It allows access to application-specific resources and classes, as well as up-calls for application-level operations such as launching activities, broadcasting and receiving intents, etc.
  	  					
Resources Activity.getResources()/SharedPreferences Activity.openFileInput openFileOutput
android.util.Log --->LogCat

/////////////////////////////////////////////////
GPIO General Purpose Input Output / 对板子编程
TTL Time To Live IP
TTL Transistor Transistor Logic
LVDS Low Voltage Differential Singaling

TTL--->LVDS Sender--->LVDS Receiver--->TTL
N/A Not Available
////////////////////////////////////////////////

Java Comment/Annotation JavaDoc JavaC Java JavaW CLASSPATH

The virtual machine's built-in class loader, called the "bootstrap class loader
/////////////////
Android RPC AIDL
AppDomain(.Net) Sandbox(Android)

Android App可以分区，UI用Java(SDK)开发(Dlvik VM),性能部分用C/C++开发(NDK)/JNI Call(JNA)
glibc、libc.so标准C库 libm.so数学 libpthread.so libusb libupnp libwww libz

嵌入式C/C++库 http://www.uclibc.org/ uClibc uClibc++
Workbench Benchmark基准检测程序

		       				 ----X0.apk(至少一个线程)	
		                                |		
Zygote Service Process--->Android Process(VM GC)---
		      				|
			            	         ----Xn.apk(至少一个线程)	

Service与其他应用组件一样都是运行在主线程中的，如果Servcie很耗时或要阻塞，需要开子线程来单独运行服务，新建线程并在线程内启动服务
Local Service / startService( ) stopService( ) / 同一Application内通信。Remote Service / bindService( ) unbindService( ) RPC / 跨Application之间通信 / android.app.IntentService实现了线程封装

BusyBox 是一个集成了一百多个最常用linux命令和工具的软件。
Source Insight是一个面向项目开发的程序编辑器和代码浏览器，它拥有内置的对C/C++, C#和Java等程序的分析。

Google Android开发入门与实战.pdf p224
android-src.pdf P83

Android开发从入门到精.pdf p30
Android开发入门经典.pdf p27

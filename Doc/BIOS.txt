PC BIOS--->UEFI

BIOS 启动 硬件检测 初始化，OS--》BIOS--》Hardware Assembly 中断、硬件端口操作

UEFI Driver/Protocol方式
UEFI Images--> 
UEFI Applications 硬件初始化完，操作系统启动之前的核心应用
OS Loaders 负责启动操作系统并退出关闭UEFI应用
UEFI Drivers　提供设备间接口协议，不需要操作系统的支持可以操作设备

PEI EFI预初始化 并加载 DXE 驱动执行环境

http://www.bios.net.cn
BIOS (包含 ISR / Setup / POST / Bootstrap Bootloader )程序) CMOS
BIOS系统启动自举程序，系统在完成POST自检后，ROMBIOS就首先按照系统CMOS设置中保存的启动顺序搜寻软硬盘驱动器及CD―ROM、网络服务器等有效地启动驱动器，读入操作系统引导记录，然后将系统控制权交给引导记录，并由引导记录来完成系统的顺利启动。

POST 硬件检查-->Bootstrap 引导(读CMOS)-->MBR-->Boot Managner-->OS Loader
ISR 中断处理 / Setup 操作CMOS数据


App-->OS API-->Driver-->BIOS
App-->DOS( DOS Interrupt call )-->BIOS( BIOS Interrupt call )--->Hadrware